const express = require('express');
const router = express.Router();
const recipeController = require('../controllers/recipeController');

// Define routes
// Example: router.get('/', recipeController.getAllRecipes);

module.exports = router;
